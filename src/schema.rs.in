#[derive(Serialize, Deserialize, Debug, PartialEq)]
pub struct User {
    pub name: String,
    pub age: i8,
}

impl User {
    pub fn new() -> User {
        User {
            name: "testing".to_string(),
            age: 99,
        }
    }
}